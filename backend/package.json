{
  "name": "vestra-backend",
  "version": "1.0.0",
  "description": "VESTRA — Ulgurji kiyim CRM (Backend)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --check server.js && node --check db.js && node --check middleware/auth.js && node --check routes/auth.js && node --check routes/customers.js && node --check routes/orders.js && node --check routes/products.js && node --check routes/reports.js && node --check routes/dashboard.js && echo All files OK"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
